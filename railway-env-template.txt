# Railway Environment Variables Template
# Copy these to your Railway project variables

APP_NAME="Transport Fleet Management"
APP_ENV=production
APP_DEBUG=true
APP_URL=https://trolley.proxy.rlwy.net

# Database Configuration (SQLite)
DB_CONNECTION=sqlite
DB_DATABASE=/app/database/database.sqlite

# Cache and Session
CACHE_DRIVER=file
SESSION_DRIVER=file
QUEUE_CONNECTION=sync

# Logging
LOG_CHANNEL=stack
LOG_LEVEL=debug

# Mail (Optional)
MAIL_MAILER=log
MAIL_HOST=localhost
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="noreply@trolley.proxy.rlwy.net"
MAIL_FROM_NAME="${APP_NAME}"
